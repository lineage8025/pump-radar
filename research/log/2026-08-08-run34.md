# 2026-08-08 run34 — 實驗開始（骨架）

> 本檔為 `program.md` §8.4 要求的「開跑前骨架日誌」，job 被硬砍時供下輪接續。
> 完成後本段會被完整日誌取代。

## 車道與問題

- 車道：`audit`（單一車道）
- 使用者指定 focus：**Q10**
- 新問題 **Q22**：`CLAUDE.md` 2026-08-08「結算」段中**與 Q10 有關的宣稱**之內部一致性稽核。
  - 該段文字是 commit `dc14f97` 之後才出現的**全新內容，從未被任何一輪稽核過**，
    且 CLAUDE.md 是所有未來讀者最先載入的檔案、該段自身要求「引用時必須一併帶上」，
    故其數字的準確性會被大量複製。
  - 首要標的（focus=Q10）：「方向二振幅形狀**經濟門檻差一個數量級**（Q10，**三重驗證**）」。
  - `audit` 車道定義：只覆核既有主張、不提新主張。**本輪不新增任何格位（`[cells=0]`）**，
    方向二累計 M 維持 24。

## 預計步驟

1. 由凍結產物 `research/experiments/2026-08-05_q10_shape_grid.tsv`（run8 的 24 格表）
   重算 `cap = ΔR × E[A+B]` 的全網格最大值與其對 0.28%／0.20%／0.10% 三個門檻的比值。
2. 對照 LEDGER run8/13/18/22/28 各自記錄的 cap 點估計與調整後 CI 上界，檢查
   「差一個數量級」在**點估計**與**CI 上界**兩種讀法下各自成立與否。
3. 清點 Q10 實際被獨立稽核過幾輪、被幾條獨立資料路徑重現過，核對「三重驗證」。
4. 順帶核對同段其餘可機械驗證的算術宣稱（毛報酬 +0.030%、bracket 恆等式、
   forward 三分級數字對 `docs/DETECTOR_PREREG.md`「結算」段與 run27 的一致性）。
5. 查證資金費率 carry「+7.53%/年」在本 repo 內是否有可引用出處。

## 資料範圍

**不抓取任何新 K 線資料**，只讀 repo 內既有產物與文件。
故**不觸碰 `docs/TRADEABILITY_PREREG.md` §4 的 holdout 封存段**（2025-07-01 之後）。
run8 產物本身的窗口是 2023-01-01~2025-06-30（暖機 2022-12），已由 run13 §結構檢定
確認最後錨點為 2025-06-29 02:45、程式層不可能跨越封存段。

## 命題界定聲明（`TRADEABILITY_PREREG.md` §1.3）

本輪**未提出任何新命題**——所有被檢查的數字都是既有文件已宣告的值，
稽核只問「文件內的數字彼此對不對得起來」。符號翻轉檢定的受理／不受理判定
沿用 Q10 原題（R 對報酬乘 −1 不變 ⇒ 振幅／路徑命題，受理），本輪不改變其射程。
