# 2026-08-07 run21 — 實驗開始骨架

## 車道與問題

`probe`（非假設檢定，方法論基礎建設）— **Q11 方向三：aggTrades 資料管線擴大標的涵蓋（10/10）**。

## SETUP 檢查結果

- `.claude/agents/researcher.md`、`docs/TRADEABILITY_PREREG.md`、`CLAUDE.md`、
  `docs/DETECTOR_PREREG.md`、`research/program.md`、`docs/RND_BACKLOG.md` 已讀。
- `research/backlog.md`、`research/LEDGER.tsv` 與最近兩份日誌
  （`2026-08-07-run19.md`、`2026-08-07-run20.md`）已讀。
- **run20 只提交骨架日誌（commit `8a8c225`）即中斷，無對應 ledger 行** → 依 `program.md` §1.3
  已於本輪開頭補記 `status=error` 一行（LEDGER run20）。
- 完整性檢查：本地為 shallow clone，`git log` 無法覆核多輪歷史；改核對內容本身——
  `scripts/detector.py` 的 `PARAMS`（`bbw_pct_window=2880`、`bbw_pct_min_periods=960`、
  `squeeze_pct=0.25`）與 `research/program.md`／`docs/RND_BACKLOG.md` 引用數字一致，
  `data/insample_scored.tsv` 本輪未讀取內容。**環境限制，非發現異常**，不構成停止條件。

## 判定「這輪做什麼」

沿用 run20 骨架的判斷（未變）：Q10 已 `weakens` 並經兩輪獨立稽核確認；Q7/Q12 待人類裁決；
Q4 待人類 review；Q5 `watching` 無新輸入。**Q11 方向三仍有明確未完成覆蓋**：
`docker-compose.yml` 10 個 PAIRS 中，Phase A 對 2024-01~03 窗口目前覆蓋 6/10
（BTC/ETH/SOL/XRP/LINK/AVAX），缺 ADA/DOGE/BNB/LTC 4 個標的——`program.md` §2.1
明文指定「沒事做時優先撿」的不消耗格位預算基礎建設，本輪據此執行。

## 計畫步驟

1. Q11 Phase A：`research/experiments/2026-08-05_q11_aggtrades_pipeline.py`
   對 `ADA/USDT,DOGE/USDT,BNB/USDT,LTC/USDT` × `2024-01~2024-03` 執行
   （逐日下載→聚合→丟棄，不落地原始檔）。
2. 交叉驗證：至少對新增標的之一，用聚合後 1m 摘要 resample 成 15m，
   與 `research/fetch_klines.py` 抓的官方 K 線逐根比對（沿用 run10/17 方法）。
3. Q11 Phase B：若 Phase A 全數完成，唯讀 import
   `2026-08-06_q11_phaseB_realpath_grid.py`（沿用 run17 `2026-08-07_q11_phaseB_6pairs.py`
   的覆寫模式），覆寫 `PAIRS` 為全 10 標的，重跑頭條格位（W=4.6/N=80/hold/L=30）
   與 115 個 Q9 翻轉格位，完成 2024-01~03 窗口的標的維度全覆蓋（10/10）。
4. 若時間或資料不足以完成步驟 3，Phase A 完成本身即有獨立記錄價值，
   將如實記錄進度並收工，不勉強湊 Phase B。

## 資料窗口聲明（TRADEABILITY_PREREG §6.2）

僅使用 2024-01-01~2024-03-31（既有窗口，在 2023-01-01~2025-06-30 in-sample 範圍內）。
**未觸碰 2025-07-01 之後的封存段。**

## 格位聲明

本輪為 Q11 方法論基礎建設，不做任何方向性假設檢定，**[cells=0]**，
方向二累計 M 維持 24（不受影響）。

---
（本檔為運行開始時的骨架，若 job 中途中斷，下一輪將依 `program.md` §1.3 補記
`status=error` 的 ledger 行；正常完成則本檔會被覆寫為完整日誌。）
