# 2026-08-02 研究日誌（run 5，手動觸發，focus=Q2''）

> 骨架先行 commit（`program.md` §8.4）：長任務（10 標的 × 43 個月度 zip 抓取 ＋ 3 個窗口
> × 全網格重跑 ＋ 逐週區塊 bootstrap）開跑前先寫入問題 ID、預計步驟與資料範圍，
> job 被硬砍時下次運行接得上。結果段於跑完後補記。

## 本次車道與問題

- 車道：`probe`（單一車道，不併其他車道——Q2'' 本身是全網格 × 3 窗口重跑，工作量足夠一輪）
- 問題：**Q2''**（`research/backlog.md`，2026-08-01 run3 提出、2026-08-02 人類裁決放行並登記窗口）
- 授權與約束：`research/program.md` §4.2「窗口延長協定」四條——全網格重跑／原窗口仍是主結果／
  必須加做時期分層／窗口先寫進 backlog 才准跑（已於 2026-08-02 由人類寫入 backlog Q2''）。

## 預計步驟（骨架階段先寫死，防事後改口徑）

1. 抓 10 標的 `2022-12 ~ 2026-06` 月度 15m（`research/fetch_klines.py` 的
   `fetch_month` + `to_candles`，逐月呼叫；PR #4 已修 ms/µs 判斷，run1~3 的探針內繞法不再需要）。
2. `detector.add_indicators(candles, PARAMS)` 唯讀複用，丟棄 `bbw_pct` 為 null 的列。
3. 三個窗口各自**獨立**跑完整流程（去叢集掃描各自從窗口起點重新開始，不切片共用）：
   - `ORIG` = 2025-01-01~2026-06-30（**主結果**；同時是時期分層的後段 P2；預期重現 run1 的 n=5450）
   - `EXT` = 2023-01-01~2026-06-30（事後延長，並列呈現）
   - `P1` = 2023-01-01~2024-12-31（時期分層前段＝新增期）
4. 每個窗口輸出：(a) Q1 凍結網格六分桶 × T{1,4,12,24h} × {p50,p80,p95}＋每格 n；
   (b) 最低桶 vs 最高桶 p80 差距＋逐週區塊 bootstrap 95% CI（沿用 Q1' 方法，2000 次）；
   (c) Q2' event 分桶配對表（event vs 同桶隨機時點 24h p80）＋跨桶合併＋A 級合併。
5. 主要待答：**極端壓縮兩桶（<10%）的 event n 在延長窗口後是否過 30**（Q2' 遺留缺口）、
   以及 **Q1 效應在 P1／P2 兩個不重疊時期是否各自成立**（run1 最強反駁第 1／3 點：宏觀 regime 混淆）。

## 凍結項（本次一律不動）

分桶（六桶）、T（1/4/12/24h）、度量 `(max(high)−min(low))/close`、去叢集（取樣點間隔 ≥96 根、
event 端同標的 cooldown 16 根＋跨標的同 4h 窗僅計首發）、標的（docker-compose 的 10 PAIRS）。
`scripts/detector.py` 的 `PARAMS` 只讀不改。

## 資料來源與範圍

（跑完後補記：各標的實際根數、起訖、缺月、bbw_pct null 數、event 原始數）

## 結果表

（跑完後補記）

## 發現

（跑完後補記）

## 限制段

（跑完後補記；至少含 §4 的 Parkinson 零漂移偏誤）

## 最強反駁

（跑完後補記）

## backlog 更新摘要

（跑完後補記）

## 誘惑清單

（跑完後補記）

## 下次建議起點

（跑完後補記）
