# 2026-08-01 研究日誌（run 1，手動觸發）

> 骨架版本——長任務（抓資料＋跑探針）開始前先 commit，供 job 中途被砍時下次接續。
> 完整內容將在運行結束前補齊本檔。

## 本次車道與問題

- `probe` Q1（主業，優先序最高，backlog 全新未動）：Phase 0——`bbw_pct` 六分桶 × T=1/4/12/24h
  × p50/p80/p95 振幅分位數表，凍結網格見 `research/program.md` §4。
- `audit` Q3（次要，資料已在 repo 內、成本低）：`docs/DETECTOR_PREREG.md` 樣本內基準
  可重算性覆核，對照 `data/insample_scored.tsv`。

## 資料來源與範圍（預計）

- 標的：`docker-compose.yml` 的 10 個 PAIRS（BTC/ETH/ADA/SOL/XRP/DOGE/BNB/LINK/LTC/AVAX，皆 /USDT）。
- 分析窗口（`research/backlog.md` Q1 登記）：2025-01-01 ~ 2026-06-30。
- 抓取範圍：`research/fetch_klines.py --start 2024-12 --end 2026-06`（暖機月 2024-12，
  滿足 `bbw_pct_window=2880` 根/30 天需求），落地 `/tmp/kl_phase0/`。
- 分析時丟棄 `bbw_pct` 為 null 的列（不當 0 處理）。

## 預計步驟

1. 抓 10 標的 feather。
2. 探針腳本 `research/experiments/2026-08-01_phase0_bbw_amplitude.py`：
   `add_indicators` → 依窗口切片 → 六分桶 × 4 個 T → 去叢集（同 4h 窗僅計首發）→ p50/p80/p95 + n。
3. Q3 audit 腳本 `research/experiments/2026-08-01_q3_audit_insample.py`：重算
   `data/insample_scored.tsv` 的 net_24h mean/median/win、A/B 級 MAE 中位數，比對 DETECTOR_PREREG 數字。
4. 補完本日誌全文（結果表、發現、限制段、最強反駁）、更新 LEDGER.tsv 與 backlog.md、commit push、
   開 issue、發 Discord。

（以下待補）
