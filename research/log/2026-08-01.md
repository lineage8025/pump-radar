# 2026-08-01 研究日誌（run 1，手動觸發）

## 本次車道與問題

- `probe` **Q1**（主業，backlog 全新未動，優先序最高）：Phase 0——給定 `bbw_pct` 六分桶，
  未來 T=1/4/12/24h 最大振幅 `(max(high)-min(low))/close` 的分位數分佈。凍結網格見
  `research/program.md` §4，本次探針一字未動網格。
- `audit` **Q3**（次要，資料已在 repo 內、成本低）：`docs/DETECTOR_PREREG.md` 引用的樣本內
  基準能否用 `data/insample_scored.tsv` 逐筆重算出來。

兩車道皆在 §2「至多選 2 個車道」限制內。SETUP 完整性檢查（`git log` 查
`docs/DETECTOR_PREREG.md`／`research/program.md`／`scripts/detector.py`／
`data/insample_scored.tsv` 的修改歷史）：僅有一筆初始建檔 commit（`1f1eda3`），無異常改動，
`detector.py` 的 `PARAMS` 與 `DETECTOR_PREREG.md` 參數指紋一致。`research/LEDGER.tsv`、
`research/log/`、`research/experiments/` 皆為空（僅 README），backlog.md 各問題狀態如檔案所載，
確認本次是**第一次真正執行的運行**，無未完成實驗需要接續。

## 資料來源與範圍

- 標的：`docker-compose.yml` 的 10 個 PAIRS（BTC/ETH/ADA/SOL/XRP/DOGE/BNB/LINK/LTC/AVAX，皆 /USDT）。
- 分析窗口（`research/backlog.md` Q1 登記）：**2025-01-01 ~ 2026-06-30**（546 天）。
- 抓取範圍：`2024-12 ~ 2026-06`（暖機月 2024-12，滿足 `bbw_pct_window=2880` 根/30 天）。
- 每標的抓得 **55,392 根** 15m K 棒（2024-12-01 00:00 ~ 2026-06-30 23:45 UTC），
  分析窗口內（含暖機後）每標的 **52,416 根**皆有非 null 的 `bbw_pct`（100%，因暖機月已足月，
  無需額外丟列）。

### 資料管線 bug（發現，需人工修復）

`research/fetch_klines.py` 對本迴圈**唯讀**，不得修改。實測發現其 `to_candles()` 只用整批
concat 後的**第一筆** `open_time` 判斷 ms/µs 單位；Binance 從 2025-01 起把 monthly klines
的時間戳從 ms（13 位數）換成 µs（16 位數），但暖機月 2024-12 仍是 ms
（實測 `BTCUSDT-2024-12` `open_time`=13 位數，`BTCUSDT-2025-01` 起=16 位數）。
一次抓「2024-12~2026-06」整段時，該函式用 2024-12（ms）的單位去解析全部 19 個月，
把 2025 年之後的資料解析成西元 58466 年，直接觸發原檔第 131 行 `strftime` 例外
（`--pairs BTC/USDT,...,AVAX/USDT --start 2024-12 --end 2026-06` 對全部 10 標的皆重現）。

**繞法（不修改該檔，只在本次探針裡逐月正確呼叫其未改動的 `fetch_month()`）**：
`research/experiments/2026-08-01_phase0_bbw_amplitude.py` 的 `month_to_candles()` 對每個月
的原始 CSV 各自判斷單位再 concat，抓取來源與方式（`data.binance.vision` 月度 zip）與原檔
完全一致，只是把「單位判斷」從批次層級搬到月份層級。**這是本次運行對唯讀檔案唯一的偏離**，
偏離內容全文寫在探針腳本開頭 docstring，供覆核。**建議事項**：這個 bug 會影響任何跨
2024-12→2025-01 邊界抓資料的未來研究，值得開一個小 PR 修 `to_candles()`（逐列而非逐批判斷
單位）——本次運行未開此 PR（時間分配優先給 Q1/Q3 本體），留在下方「誘惑清單」供下次評估。

## 結果表（Phase 0，去叢集後 n=5450，凍結網格 6 桶 × T{1h,4h,12h,24h} × {p50,p80,p95}）

去叢集規則：每標的獨立處理，取樣點間隔 ≥ 96 根（=24h=最大 T），沿用
`DETECTOR_PREREG.md` 已預登記的「同視窗僅計首發」精神並套用到全網格（非僅 event）；
每標的 52,416 根候選 → 545 或 546 個獨立取樣點（末端不足 96 根前瞻窗口者捨去）。
桶界依 `program.md` §4 原文「[0,0.05)…[0.75,1.0]」，左閉右開、末桶兩端皆閉。
振幅單位：% （已 ×100）。

| bucket | n | 1h p50 | 1h p80 | 1h p95 | 4h p50 | 4h p80 | 4h p95 | 12h p50 | 12h p80 | 12h p95 | 24h p50 | 24h p80 | 24h p95 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [0.00,0.05) | 360 | 0.630 | 1.007 | 1.582 | 1.241 | 1.962 | 3.073 | 2.316 | 3.516 | 5.388 | 4.046 | 6.351 | 9.799 |
| [0.05,0.10) | 326 | 0.689 | 1.235 | 1.916 | 1.339 | 2.368 | 3.502 | 2.516 | 4.112 | 5.853 | 4.181 | 6.836 | 10.808 |
| [0.10,0.25) | 926 | 0.779 | 1.272 | 2.218 | 1.512 | 2.526 | 4.405 | 2.669 | 4.346 | 6.397 | 4.486 | 7.387 | 11.672 |
| [0.25,0.50) | 1397 | 0.892 | 1.428 | 2.468 | 1.755 | 2.822 | 4.437 | 2.992 | 4.640 | 6.995 | 4.878 | 7.508 | 11.399 |
| [0.50,0.75) | 1270 | 1.040 | 1.682 | 2.621 | 2.001 | 3.210 | 5.071 | 3.152 | 5.121 | 8.015 | 5.161 | 8.036 | 13.035 |
| [0.75,1.00] | 1171 | 1.320 | 2.177 | 3.609 | 2.407 | 3.972 | 6.727 | 3.561 | 5.936 | 10.957 | 5.772 | 9.111 | 15.251 |

**對照組（24h p80，最低桶 vs 最高桶，percentile bootstrap 2000 次重抽，seed 固定）**：
最低桶 n=360，最高桶 n=1171，點估計差距 **+2.760pp**（最高桶大於最低桶），
**95% CI [+2.138, +3.464]pp，下界 >0**——原始資料與明細見
`research/experiments/2026-08-01_phase0_bbw_amplitude_result_table.tsv`（彙總表）與
`_samples.tsv`（5450 筆逐樣本明細，含 pair/ts/bucket/四個 T 的振幅）。

## 發現

1. **`bbw_pct` 對後續振幅有清楚區別力，六個桶在全部 4 個 T 上單調遞增，且 24h 檔 CI 下界 >0**——
   不觸發 `docs/RND_BACKLOG.md` 的 kill criteria（「各分桶分佈重疊、24h 也無區別力」未成立）。
   Q1 的問題「`bbw_pct` 對後續振幅有無區別力」**已證實有**，Phase 0 驗收達成：能讀出
   「壓縮狀態下，區間放多寬才有 80% 機率撐過 T 小時」（例如最低桶 24h p80=6.35%，
   最高桶 24h p80=9.11%）。
2. **但方向與「壓縮→反轉爆發」的直覺相反，且持續到 24h、未見反轉**。`docs/RESEARCH_BBW_VOLATILITY.md`
   的解讀紀律預告「短 T 低分桶振幅更小是預期內」——1h/4h 確實如此，這部分**符合預期，不是
   意外**。但該文件同時預期「均值回歸力隨壓縮極端程度增強，24h 檔可能出現反轉訊號」；
   本次數據**沒有看到這個反轉**：全部 6 個桶、全部 4 個 T（含 24h）都呈單調遞增，
   最極端的 `<5%` 桶不但沒有比 `5~10%` 桶振幅更大，反而全程最小（1h p50 0.630% 到 24h p80
   6.351%，六桶中最低）。也就是說，本次結果支持的機制是「波動持續性主導了整個測試窗口
   （含 24h）」，不是「均值回歸在極端壓縮桶反超持續性」——這兩個文件已預告過的敘事在
   Phase 0 的實證上**沒有長得一樣**，方向乾淨地只支持前者。
3. **這對 Q2（Phase 0.5，`event` 是否為波動爆發預警）是警訊而非佐證**。`event` 的 A 級門檻
   是「觸發前一根 `bbw_pct` ≤25%（壓縮）」，若壓縮本身預示的是「接下來振幅偏小」而非
   「接下來振幅偏大」，Phase 0.5 直接比較「`event` 觸發後 24h 振幅」vs「隨機時點 24h 振幅」
   時，`squeeze_before` 這個前置條件可能對振幅是負向貢獻，需要靠 `event` 定義裡另外的
   `vol_z≥3.0` 放量條件才能把振幅拉回正向——Q2 執行時應該把這點寫進解讀，不能只看
   「event 後振幅 > 隨機振幅」就直接歸因給壓縮。
4. **對網格交易 Phase 0 的實務意涵是正面的，但敘事要修正**：不是「壓縮之後要準備迎接爆發、
   網格要留更寬緩衝」，而是「當下 `bbw_pct` 越低，24h 內区間可以設得越窄且仍有 ~80% 機率
   不被打穿」——這是一個**可直接查表使用的動態定寬依據**，比「等反轉」更直接可操作。

## 限制段

- **Parkinson 型未修正極值估計量，零漂移假設**（`program.md` §4 已預告，必須寫）：
  `(max(high)-min(low))/close` 對漂移項敏感。本次高分桶（`bbw_pct` 高）很可能同時是
  「近期已在趨勢/波動擴張狀態」的標的，若這些時段本身單邊行情多，range 會系統性偏高，
  把「有方向的趨勢延續」誤記成「振幅擴大」——這正是網格交易最怕的情境，本結果**無法
  排除**這個混淆：`bbw_pct` 高與「即將發生的無方向大波動」和「已經在單邊趨勢中」在本
  分析框架下無法區分。
- **未做 Yang-Zhang/Garman-Klass 交叉驗證**（`RESEARCH_BBW_VOLATILITY.md` §5 建議 4）：
  本次只算了原始 Parkinson 型度量，沒有算修正版做一致性檢查，時間分配優先給主表本身。
- **去叢集只做到「同標的」層級，未做跨標的去叢集**：12 個標的中若干在同一天可能因大盤
  同向波動而同時落入相似的 `bbw_pct` 桶且振幅相關（如全市場齊漲跌日），5450 筆「獨立
  樣本」的**有效自由度低於表面 n**，見下方最強反駁第 1 點。
- **未做大小市值標的分開跑**（`RESEARCH_BBW_VOLATILITY.md` §5 建議 5）：10 標的合併計算，
  流動性系統差異（BTC/ETH vs LTC/AVAX 等）造成的 range 低估程度未拆解。
- **`bbw_pct` 與振幅度量在機制上部分同源**：`bbw_pct` 本身是 20 期收盤價標準差的百分位，
  與「未來 high-low range」都是同一族「近期價格分散程度」的度量，1h/4h 檔的區別力
  某種程度上是「安靜的還會再安靜一陣子」這種接近機械性的短期延續，非全新發現；
  真正有資訊量的是它延續到 24h 沒有衰減——這點在下方最強反駁進一步討論。

## 最強反駁

1. **有效樣本數遠小於 5450**。去叢集只保證同一標的內取樣點間隔 ≥24h，但沒有跨標的去叢集。
   若 2025-01~2026-06 這 18 個月裡大盤只經歷了少數幾次「全市場齊壓縮」「全市場齊放量」的
   宏觀 regime 切換（例如某幾週的普遍盤整、某幾週的普遍趨勢），則 10 個標的在同一時期會
   高度相關地落入相似的桶並產生相關的振幅結果——本質上是用几個獨立的宏觀事件冒充了
   5450 個獨立觀測。這會讓 bootstrap CI **系統性低估真實不確定性**，[+2.138,+3.464]pp
   這個看起來很緊的區間可能過度自信。**若這個反駁成立，Q1「有區別力」的方向判斷未必錯，
   但信賴區間的緊度不可信**，下次運行應該用「按週或按月分塊」的 block bootstrap 重估，
   而不是逐樣本重抽。
2. **同源度量的機械性延續，而非新發現的「訊號」**：`bbw_pct` 是收盤價短期標準差的百分位，
   下一刻的 high-low range 本質上也是短期標準差的另一種量化——兩者用同一批鄰近的 K 棒
   算出來，1h/4h 甚至 12h 檔的相關性可能只是同一個底層波動狀態被兩種公式各測了一次，
   不是「壓縮預示了什麼」。這個反駁對 24h 檔威力較弱（12h→24h 已經是 96 根之外的全新
   K 棒，重疊視窗的直接算術污染在去叢集後應已排除），但仍無法完全排除「同一個持續了
   數天到數週的宏觀波動 regime」同時解釋了 `bbw_pct` 桶與 24h 後的振幅——與反駁 1 本質
   是同一件事的兩種講法。
3. **若反駁 1+2 都成立**：Q1 的正確結論會從「已證實有區別力」降級為「**未證實**——觀察到的
   單調關係可能是少數宏觀 regime 的偽相關，需要 block bootstrap 或按季度/按標的拆分重估
   才能判斷是否在扣掉 regime 相關性後依然成立」。**這是本次運行寫得最不舒服但最誠實的
   一句話**：目前的證據強度撐得住「有區別力，方向是持續性不是反轉」這個描述性結論，
   但撐不住「這是一個可獨立於宏觀 regime 使用的通用規律」這個更強的結論。

## backlog 更新摘要

- **Q1**：由 `open` → `closed`。結論：`bbw_pct` 對後續振幅有區別力（24h 檔 bootstrap CI
  下界 >0），方向為持續性而非反轉，Phase 0 驗收達成，不觸發 kill criteria。已移入
  `backlog.md`「已結案」段，日期 2026-08-01，連結本檔與 `research/experiments/
  2026-08-01_phase0_bbw_amplitude*`。**保留最強反駁第 3 點的降級可能性**，若下次 block
  bootstrap 覆核後區別力消失，需要在 ledger 補記並更新本結論，不算「改結果重跑」
  （這是同一結論的穩健性覆核，不是換分桶/換 T/換標的）。
- **Q2**：由 `watching`（等 Q1）→ `open`（解封）。已在上方「發現」第 3 點附加提醒：
  執行時需把「壓縮本身可能對振幅是負向貢獻」這個 Q1 新發現寫進 Q2 的解讀，不能只看
  「event 後 > 隨機」的表面比較。
- **Q3**：由 `open` → `closed`。結論：**完全重算一致，無差異**——樣本內基準表
  （原 4 標的 107 事件：net_24h mean −0.31%/median −0.36%/win 39%/n=102、A 級 mean −0.27%/
  win 36%/n=28、mfe_24h mean +1.81%、mae_24h mean −2.12%）與全 10 標的 235 事件分級分佈表
  （A n=64/B n=171 及各項中位數、尾部佔比）**逐項核對均與 `DETECTOR_PREREG.md` 引用數字
  完全相符**，見 `research/experiments/2026-08-01_q3_audit_insample.py` 輸出。

## 誘惑清單

- 開一個小 PR 修 `research/fetch_klines.py` 的 `to_candles()`，把 ms/µs 單位判斷從
  「整批第一筆」改成「逐月/逐列」——本次已在探針裡繞過，但這個 bug 會擋住任何未來
  跨 2024-12→2025-01 邊界的資料抓取，值得修。**本次未做**（判斷優先給 Q1/Q3 本體，
  且怕在同一次運行內既繞過又急著改管線分散注意力，PR 本身也需要留時間讓人 review）。
- 用 block bootstrap（按週或按標的分塊）重估 Q1 的 CI，直接回應「最強反駁」第 1 點——
  這**不是**「換分桶/換T重跑」，是同一分析的穩健性覆核，下次運行可以做。
- Yang-Zhang 或至少 Garman-Klass 版本重算一次振幅度量，交叉驗證 Parkinson 版本的方向
  是否一致（`RESEARCH_BBW_VOLATILITY.md` §5 建議 4）。
- 大小市值標的分開看（BTC/ETH vs LTC/AVAX 等），檢查區別力是否被流動性混淆
  （§5 建議 5）。

## 下次建議起點

優先序：① Q2（Phase 0.5，現已解封）；② 上方誘惑清單的 block bootstrap 穩健性覆核
（直接回應本次最強反駁，價值高於開新方向）；③ fetch_klines.py bug 的 PR（次要，不擋
研究本身）。若人類這幾天看到本日誌後決定「最強反駁」已經動搖 Q1 結論，下次運行應該
先處理那個覆核，而不是急著往 Q2/Phase 1 推進。
