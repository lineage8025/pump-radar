# 2026-08-07 run20 — 實驗開始骨架

## 車道與問題

`probe`（非假設檢定，方法論基礎建設）— **Q11 方向三：aggTrades 資料管線擴大標的涵蓋**。

## SETUP 檢查結果

- `CLAUDE.md`、`docs/DETECTOR_PREREG.md`、`docs/TRADEABILITY_PREREG.md`、
  `research/program.md`、`docs/RND_BACKLOG.md` 已讀。
- `research/backlog.md`、`research/LEDGER.tsv` 與最近兩份日誌
  （`2026-08-07-run18.md`、`2026-08-07-run19.md`）已讀，無未結案骨架殘留
  （run18/19 皆有對應 ledger 行）。
- 完整性檢查：本地為 shallow clone（`git rev-parse --is-shallow-repository` = true，
  僅 1 個 commit 可見），**無法用 `git log` 覆核 `PARAMS`／預登記檔的歷史變更**——
  改核對內容本身：`scripts/detector.py` 的 `PARAMS`
  （`bbw_pct_window=2880`、`bbw_pct_min_periods=960`、`squeeze_pct=0.25`）與
  `research/program.md`／`docs/RND_BACKLOG.md` 引用數字一致，`data/insample_scored.tsv`
  未被寫入（本輪未讀取內容，僅檢查未變動軌跡不可得）。**此為環境限制而非發現異常**，
  照實記錄，不視為停止條件。

## 判定「這輪做什麼」

- Q10（方向二 Phase 0）已 `weakens` 並經兩輪獨立稽核（Q13/Q14）確認，無新工作。
- Q7／Q12 皆待人類裁決，agent 不得自行執行。
- Q4（tooling PR #8）待人類 review，非本輪可推進項目。
- Q5（lit）watching，無新外部輸入觸發。
- **Q11 方向三仍有明確未完成覆蓋**：`docker-compose.yml` 10 個 PAIRS 中，
  Phase A 對 2024-01~03 窗口目前只覆蓋 6/10（BTC/ETH/SOL/XRP/LINK/AVAX），
  缺 ADA/DOGE/BNB/LTC 4 個標的；這是`program.md` §2.1 明文指定「沒事做時優先撿」
  的不消耗格位預算的基礎建設，本輪據此執行，非硬找事做。

## 計畫步驟

1. Q11 Phase A：`research/experiments/2026-08-05_q11_aggtrades_pipeline.py`
   對 `ADA/USDT,DOGE/USDT,BNB/USDT,LTC/USDT` × `2024-01~2024-03` 執行
   （逐日下載→聚合→丟棄，不落地原始檔）。
2. 交叉驗證：至少對新增標的之一，用聚合後 1m 摘要 resample 成 15m，
   與 `research/fetch_klines.py` 抓的官方 K 線逐根比對（沿用 run10/17 方法）。
3. Q11 Phase B：若 Phase A 全數完成，唯讀 import
   `2026-08-06_q11_phaseB_realpath_grid.py`，覆寫 `PAIRS` 為全 10 標的，
   重跑頭條格位（W=4.6/N=80/hold/L=30）與 115 個 Q9 翻轉格位，
   完成 2024-01~03 窗口的標的維度全覆蓋（10/10）。
4. 若時間或資料不足以完成步驟 3，Phase A 完成本身即有獨立記錄價值，
   將如實記錄進度並收工，不勉強湊 Phase B。

## 資料窗口聲明（TRADEABILITY_PREREG §6.2）

僅使用 2024-01-01~2024-03-31（既有窗口，在 2023-01-01~2025-06-30 in-sample 範圍內）。
**未觸碰 2025-07-01 之後的封存段。**

## 格位聲明

本輪為 Q11 方法論基礎建設，不做任何方向性假設檢定，**[cells=0]**，
方向二累計 M 維持 24（不受影響）。

---
（本檔為運行開始時的骨架，若 job 中途中斷，下一輪將依 `program.md` §1.3 補記
`status=error` 的 ledger 行；正常完成則本檔會被覆寫為完整日誌。）
